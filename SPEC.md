# NaviDash Product Specification

状态：有效  
规范版本：5  
最后更新：2026-08-27

## 1. 文档权威

本文件定义 NaviDash 当前产品行为和系统不变量。README 负责对外介绍，`docs/PRODUCT.md`
负责产品愿景，`docs/ROADMAP.md` 负责开发顺序，`changelog.md` 负责已完成变更。

涉及产品行为、数据结构、安全边界或兼容策略的开发，必须先与本规范一致；若产生冲突，
应先修改规范并记录原因，再实施代码。

本文使用以下关键词：

- **MUST**：实现必须满足。
- **SHOULD**：默认应满足，偏离时需要明确理由。
- **MAY**：允许实现，但不是必需能力。

## 2. 产品原则

- NaviDash MUST 是注意力优先的私人浏览器主页，而不是要求用户长时间停留的 Dashboard。
- Action 内容（搜索、链接、快速启动）MUST 优先于 Glance 内容（时间、天气、待办）和
  Ambient 内容（背景、照片）。
- 个性化 SHOULD 帮助用户更快识别下一步，而不是鼓励堆满组件。
- 自托管、备份与本地优先数据 MUST 保证用户对个人数据的控制。

## 3. 快速启动器

- 用户在画布空闲状态输入字符时，系统 MUST 打开快速启动器。
- 可见搜索入口与 `Ctrl/⌘ + K` MUST 打开同一个快速启动器，不得维护独立搜索流程。
- 页面 SHOULD 以画布为主体，不得为了启动器入口长期占用整行 Header 空间。
- 输入控件、内容编辑、组件编辑、设置和其他弹窗中的按键 MUST NOT 触发启动器。
- 第一结果 MUST 仅在用户按下 Enter 或点击后打开，不得因单字符输入自动跳转。
- 无链接匹配时 MAY 使用默认搜索引擎搜索。
- 排序 MUST 先排除与查询完全无关的链接，再使用查询关联、静态匹配、总体使用频率和
  最近访问时间排序。
- 查询关联与总体使用频率 SHOULD 使用时间衰减，使近期习惯能够逐渐覆盖旧习惯。
- 使用统计 MUST NOT 改变画布上的组件位置、尺寸或顺序。

## 4. 使用数据与隐私

- 启动器学习数据 MUST 默认只保存在当前浏览器。
- 系统只能记录由 NaviDash 发起的链接打开行为，不得读取或声称掌握完整浏览器历史。
- 用户 MUST 能清除单个链接、单个查询关联和全部学习记录。
- 备份 MAY 包含学习数据，但导入时 MUST 明确提示其中包含个人使用习惯。
- 未来的服务端同步 MUST 为显式选择，并在访问保护启用后才能开放。

## 5. Widget 与布局数据

- 新增 Widget MUST 通过统一组件库完成；点击添加与拖拽添加 MAY 并存，但不得维护第二套选择器。
- Widget 身份和配置是全局共享内容。
- 桌面和手机分别保存 Placement，渲染 Widget 只能由配置与当前端 Placement 派生。
- 持久化数据 MUST 版本化；迁移 MUST 确定、可测试且不依赖当前视口尺寸。
- Widget Placement 与配置 MUST 以同一个带 `revision` 的快照读取和写入；服务端 MUST
  原子替换快照，并拒绝基于过期 revision 的覆盖写入。
- 客户端遇到 Widget 快照 revision 冲突时 MUST 保留当前本地内容并暂停后续自动写入，
  MUST 明确提示用户选择保留本地版本或使用服务端版本；在用户选择前，页面恢复同步
  MUST NOT 静默覆盖本地内容。
- 客户端 MUST 在首次加载、页面恢复可见和网络恢复时拉取服务端数据；默认 MUST NOT
  使用持续短间隔轮询。
- 同一时刻 MUST 只允许一组后台拉取；短时间内由焦点、页面恢复等事件触发的重复请求
  MUST 合并，避免并发覆盖和无意义文件读取。
- 组件库 MUST 只提供当前产品核心组件 Links、Today、Memo、Poster、F1 和 Komari。已下线的 Clock、
  Weather、Date、Quick Link、Todo 与 Calendar 不再保留渲染器；读取旧数据时 MUST
  确定性过滤这些项，且不得影响同一份数据中的其他有效 Widget。
- Links MUST 保持用户指定的视觉顺序；使用统计不得在画布中自动交换链接位置。
- 书签库 MUST 是链接资产的唯一正式来源；快速启动器检索整个书签库，Links Widget 只按
  `bookmarkIds` 引用用户固定到首页的少量书签。
- 书签 Favicon SHOULD 在外部服务返回固定 `16×16` 占位图片时显示统一默认图标，不得将该
  占位图片伪装为网站图标。
- 从 Links Widget 移除书签 MUST 只解除当前 Widget 引用；删除 Widget MUST NOT 删除书签。
- 删除仍被 Widget 引用的书签 MUST 明确提示影响，并从相关 Widget 中确定性移除引用。
- Today MUST 在天气服务不可用时继续显示本地时间与日期，不得让网络错误破坏整个信息面板。
- Today MAY 根据用户本地日期从受控色板中确定性切换强调色；同一自然日内 MUST 保持
  一致，且不得降低文字与图标的可读性。
- 天气密钥、服务 Host 与认证方式 MUST 只由服务端环境变量配置；Widget 只能保存城市与坐标，
  客户端不得指定服务端请求目标。旧天气凭据与 Host 字段 MUST 在读取时兼容接受并确定性移除。
- Memo SHOULD 保持随手粘贴和自动保存，不得扩展成完整笔记或任务管理系统。
- Poster MUST 使用无外框的图片表面，自动轮播 MUST 默认关闭。
- Poster MAY 在原图立即可见的前提下生成接近设备显示尺寸的服务端缩略图，并在完成后无闪烁
  替换；缩略图 MUST 使用独立缓存，不得改变用户保存的原始图片地址。
- Poster 外部图片缩略图请求 MUST 只接受当前 Widget 快照中实际引用的 HTTP(S) 地址，并
  拒绝本机、内网、链路本地、保留地址及跳转后的非公开地址；下载 MUST 限制响应体积、
  图片像素、跳转次数和请求时间。
- F1 MUST 是只读的低干扰赛事摘要，默认以赛程模式突出当前时间之后的下一赛段，并 MAY 展示
  同一比赛周末的少量后续赛段；用户 MAY 将单个 F1 Widget 切换为车手积分模式，以展示当前赛季
  的最新车手积分，但不得扩展为直播比分、提醒系统或通用日历。
- F1 赛程 MUST 使用仓库内版本化的赛季数据，运行时不得要求用户提供 ICS 或访问远程赛历；
  原始 ICS SHOULD 作为可追溯来源保留，应用 MUST 读取确定性生成并经过校验的规范化数据。
- F1 大奖赛名称和地点 MUST 提供中文与英文，赛段时间 MUST 保存为 UTC 并按浏览器本地时区
  展示；待确认、取消和赛季结束状态 MUST 明确呈现，不得伪装为已确认的未来比赛。
- F1 车手积分 MUST 由服务端通过 Jolpica 获取并经过 Schema 校验，客户端不得直接访问上游；
  服务端 MUST 缓存成功结果 `24` 小时并在刷新失败时优先返回最近一次可用缓存，否则 Widget MUST
  保留布局并显示明确降级状态。积分模式表示上游最近发布的排名，不承诺比赛进行中的秒级实时性。
- Komari MUST 是只读、低干扰的节点状态摘要，默认尺寸为 `2×2`，并允许用户切换为 `2×1`
  紧凑版或 `1×1` 概览版；不得在 NaviDash 内提供远程终端、任务执行、告警管理或 Komari 管理
  能力。`2×1` MUST 保留节点身份、运行时间、三项资源百分比和可选实时速率，裁剪容量明细、累计
  流量条与更新时间；`1×1` MUST 保留节点身份、运行时间和三项资源百分比，并裁剪网络与明细。
  尺寸切换只改变展示密度，不得改变轮询、Widget 配置或状态接口。
- Komari 的 Base URL、API Key 和其他凭据 MUST 仅由服务端环境变量配置，不得写入 Widget
  配置、快照、备份、客户端存储或状态响应。每个 Widget 配置只能保存一个节点 UUID、网络速率
  显示开关和 `5`、`15`、`30` 秒三档刷新间隔；需要同时查看多台节点时 MUST 添加多个 Widget，
  不得将多张完整节点卡压缩进同一 Widget。
- Komari Widget MUST 仅访问 NaviDash 同源状态接口；服务端 MUST 对上游响应进行超时、响应
  大小和 Schema 限制，并合并短时间内的重复上游请求。可见页面中的多个 Komari Widget MUST 由
  一个共享轮询器按当前最短间隔批量请求全部活跃节点；页面隐藏时 MUST 暂停轮询，恢复可见时 MUST
  立即刷新。上游返回的 `region` 仅在其为两位 ISO 国家代码或可规范化为标准双区域指示符国旗时
  MAY 显示在节点名称前；不原生支持国旗 Emoji 的浏览器 MUST 使用随应用提供的国旗字体显示，
  不得退化为两位国家字母；
  其他地区文本 MUST 丢弃。上游不可用、数据无效或节点离线时，
  Widget MUST 保留布局并显示明确、无敏感细节的降级状态。
- Komari 完整页面 MUST 通过同源跳转端点在新标签页打开；状态接口不得返回 Komari Base URL、
  API Key、Session Cookie、上游响应头或原始错误详情。第一版 MUST 使用 HTTP 轮询，不得建立
  浏览器到 Komari 的 WebSocket 连接。

## 5.1 模板与书签导入

- 首页模板 MUST 复用正式 Widget Schema，不得维护只用于模板的第二套组件模型。
- 应用模板 MUST 明确替换当前画布，并在用户确认后一次性提交完整 Widget 快照。
- 浏览器书签导入 MUST 支持 Netscape Bookmark HTML，限制导入数量、过滤非 HTTP(S)
  地址并确定性去重。
- 书签新增 SHOULD 支持批量粘贴 Markdown 链接与纯 HTTP(S) URL，并在提交前反馈识别、
  重复与无效内容数量。
- 书签导入 MUST 写入书签库；用户 MAY 再将选中的书签固定到普通 Links Widget。
- 从 Links Widget 发起的批量粘贴 MUST 将链接写入书签库，并同时将对应引用加入当前
  Widget；从全局书签入口发起时 MUST 只写入书签库。

## 5.2 首次部署引导

- 书签导入页 SHOULD 以当前步骤作为唯一的页面级标题，并只保留一行操作说明，明确大模型
  Prompt、浏览器 HTML 和手动粘贴三种入口；桌面端主要操作区域 SHOULD 在常见视口高度内
  完整显示，避免页面级纵向滚动。
- 键盘快速启动页 SHOULD 在步骤标题下仅说明“直接输入即可开始”和当前浏览器内的使用习惯
  学习边界；动画容器底部不得重复展示这些说明。
- 服务端确认 Widget 快照从未初始化且内容为空时，首页 MUST 展示首次部署引导；数据尚在
  加载或加载失败时 MUST NOT 因客户端暂时为空而误显示。
- 引导 SHOULD 通过不修改真实数据的动画演示从组件库拖入自由画布的核心交互，并提供进入
  真实编辑模式的单一明确入口；初始化仍 MUST 复用正式 Widget Snapshot 数据契约。
- 拖拽教学前 SHOULD 允许用户粘贴由大模型整理的 Markdown 链接，或上传浏览器导出的
  Netscape Bookmark HTML；两种方式 MUST 复用正式书签解析、数量限制和去重规则。
- 大模型 Prompt MUST 根据当前语言环境自动提供推荐清单：中文界面面向中文用户，英文界面
  面向英文用户；两者均覆盖开发工具与娱乐内容并附带输出示例，不要求用户先自行整理网站。
- Prompt 内容区 MUST 在固定最大高度内支持纵向滚动，确保长 Prompt 能够完整阅读和选择。
- 首次引导右上角 MUST 提供中文与英文切换；切换 MUST 同时更新当前引导语言、对应 Prompt
  和服务端默认语言设置。用户未主动切换时，完成引导 MUST 保存当前界面语言为默认语言。
- 切换 locale 导致引导重新挂载时，当前标签页的引导步骤和尚未提交的书签 MUST 保留；完成
  引导后 MUST 清理这份临时数据。
- Prompt MUST 要求模型在纯文本代码块中仅输出可解析的“标题 | HTTP(S) URL”，避免聊天界面
  将结果渲染为超链接；不得暗示 NaviDash 会将用户书签或浏览历史发送给第三方，Prompt 的
  复制与使用由用户主动完成。
- 指引中解析的书签 SHOULD 暂存在当前会话，并在用户完成指引时随空白布局一次性写入完整
  Snapshot，避免部分初始化导致后续步骤消失。
- 首次引导 MUST 按“书签导入 → 键盘快速启动 → 组件拖拽”排列；键盘步骤 MUST 以不写入
  搜索记录、不打开真实链接的循环动画演示启动器唤起、快速输入、结果高亮和打开状态，同时
  阻止背后的真实启动器响应按键。
- 键盘动画在用户启用减少动态效果时 MUST 停留在可理解的最终静态画面。
- 键盘动画 SHOULD 先演示普通精确匹配，再输入单字符展示多个候选及访问次数，将访问最多的
  候选置顶，以可视化解释使用习惯对结果排序的影响。
- 键盘步骤 SHOULD 提醒用户启动器会依据 NaviDash 发起的链接打开习惯，在当前浏览器逐渐
  优化结果排序；不得暗示读取完整浏览器历史或已开启跨设备学习。
- 组件拖拽 MUST 作为第三步，并在完成后直接打开真实组件库，使演示与首次实际添加连续衔接。
- 组件拖拽页 SHOULD 以步骤标题、一行操作说明、全宽动画容器和容器底部完成按钮组成；不得
  重复展示 Hero 标题、功能卖点列表或动画内部步骤编号，桌面端 SHOULD 保持在单屏内。
- 首次引导作为阻断式流程时 MUST 暴露模态语义，并将键盘焦点保持在指引内部；步骤切换后
  SHOULD 将焦点移动到当前步骤标题。
- 用户开始搭建后 MUST 写入一次完整快照；完成初始化后，其他浏览器或设备 MUST NOT
  重复展示首次部署引导。

## 6. 自由画布目标规范

本节已经进入当前实现；历史单数组布局只允许通过显式迁移函数转换，当前双端布局在普通
加载、保存和视口变化中必须保持坐标。实现记录见 `docs/ROADMAP.md`，重构原因见
`docs/decisions/001-free-canvas-layout.md`。

- 桌面 MUST 使用固定 8 个逻辑列，手机 MUST 使用固定 2 个逻辑列。
- 视口变化 MUST NOT 修改持久化坐标。
- 添加或拖动发生碰撞时 MAY 触发受控布局调整：同尺寸单组件碰撞 SHOULD 优先交换，
  其他碰撞只能让受影响链保持原列向下移动。
- 一次受控碰撞最多影响 4 个现有组件；超过上限或发生越界时 MUST 显示无效预览，
  并在提交时拒绝操作、恢复原位置。
- 调整尺寸发生碰撞时 MUST 拒绝操作，不得借尺寸变化推动其他组件。
- 删除组件 MUST NOT 自动压缩布局。
- 预览 MUST 展示全部受影响组件，提交 MUST 使用同一布局计算结果。
- 保存、重载和导入 MUST NOT 产生未告知的静默重排。

## 7. 全局设置

- 系统在没有已保存语言设置时 MUST 默认使用中文；已有用户明确保存的中文或英文设置 MUST
  保持不变。
- 页面 MUST 使用统一的内部强调色；在没有清晰、全局一致的语义前，不向用户开放主题色配置。
- 设置采用自动保存时，界面 MUST NOT 提供无法撤销修改的“取消”或重复提交的“保存”操作。
- 普通外观设置 SHOULD 只暴露背景预设、背景图片、模糊和遮罩等用户可理解的能力；
  浏览器标题、Favicon 等低频能力 SHOULD 收入高级选项。
- 备份 MUST 保留桌面与手机两套 Placement 以及共享 Widget 配置，不得只从单端布局恢复。
- 恢复默认 MUST 只清理或覆盖 NaviDash 自身数据，不得调用无范围限制的存储清理操作。

## 7.1 可选访问保护

- 单用户访问保护 MUST 默认关闭，以保持仅本机部署的低摩擦体验。
- 设置服务端 `NAVIDASH_ACCESS_PASSWORD` 后，页面、数据 API 与天气代理 MUST 要求有效会话。
- 密码 MUST 只存在于服务端环境，客户端设置、备份与持久化数据不得读取或保存密码。
- 会话 Cookie MUST 为 HttpOnly、SameSite=Lax，并在 HTTPS 下启用 Secure。

## 8. 兼容与质量

- `0.7.3` MUST 作为持久化数据向前兼容的首个正式基线。后续版本修改 Snapshot、Settings、
  Backup 或客户端持久化格式时，MUST 提供从该基线或其后续版本到当前格式的确定性迁移。
- `0.7.3` 基线夹具 MUST 保持原样；未来迁移代码和测试预期应围绕夹具演进，不得通过修改
  历史夹具掩盖兼容问题。
- `0.7.3` 以前的数据 MAY 尽力读取，但不承诺保留旧画布。无法安全迁移时，系统 SHOULD
  保留可识别的数据资产并明确要求用户重新配置，不得将空白或部分丢失伪装成成功迁移。
- `0.7.3` 及之后发布的布局、设置和备份数据 MUST 保持可读取或提供显式迁移。
- Komari 是 Snapshot v2 的新增 Widget 判别类型，不改变 Snapshot、Settings 或 Backup 的外层
  数据结构，因此不提升 `schemaVersion`。这只保证新版可读取既有 v2 数据，不代表旧版可读取
  含 `komari` 的 v2 快照；直接降级运行不受支持，部署回退前 MUST 恢复旧版可读取的备份。
- F1 车手积分模式仅增加可选的 `view` 配置字段，缺失时 MUST 规范化为 `schedule`，因此不提升
  Snapshot `schemaVersion`；旧版可忽略该字段但不能呈现积分模式，回退后相关 Widget 将按旧版
  行为显示赛程。
- 读取持久化快照时，服务端 MUST 将“文件缺失”与“文件存在但 Schema 不受支持或无效”区分处理；
  后者 MUST 进入只读错误状态，绝不得回退为空快照或在后续保存时覆盖原文件。
- 已下线 Widget 与从未实现的 `rss`、`monitor` 占位项 MAY 在读取时确定性过滤，且不得影响
  同一份数据中的其他有效 Widget。
- Schema、Store、布局、导入导出和启动器排序变化 MUST 增加或更新测试。
- 合并前 MUST 通过 `npm run lint`、`npm test` 和 `npm run build`。
