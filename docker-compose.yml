version: '3'

services:
  nav2:
    build: .
    container_name: nav2
    restart: unless-stopped
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=production
