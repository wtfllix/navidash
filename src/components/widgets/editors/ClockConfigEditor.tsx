'use client';

import { WidgetConfigEditorWithSizeProps } from './types';

export default function ClockConfigEditor({
  config: _config,
  setConfig: _setConfig,
  setSize: _setSize,
}: WidgetConfigEditorWithSizeProps<'clock'>) {
  return null;
}
